# Runtime caches written by the agent-hooks at run time (never committed).
.cache/
*.done

# OS / editor noise
.DS_Store
*.swp

# Python build / test artifacts (lib/agenttools_log packaging)
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
