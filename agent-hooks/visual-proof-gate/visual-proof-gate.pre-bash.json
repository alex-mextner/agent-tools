{
  "id": "visual-proof-gate",
  "point": "pre-bash",
  "cmd": "/ABSOLUTE/PATH/TO/agent-hooks/visual-proof-gate/visual_proof_gate.py",
  "priority": 46,
  "timeout_ms": 960000,
  "on_error": "open",
  "description": "Blocks a `git commit` that stages USER-VISIBLE files (.tsx/.jsx/.vue/.svelte/.css/.scss/.html/.svg/images, or under components|ui|pages|app|views|public|assets/) unless a fresh 'looked at a screenshot' marker exists in ~/.cache/agent-tools/visual-proof/ (within VISUAL_PROOF_WINDOW_S, 3600s). No user-visible files staged => allow. The `git diff --cached` lister is timeout-bounded and fails OPEN. No self-service bypass: request a one-time Telegram approval via RIG_HATCH_REQUEST_VISUAL_PROOF_GATE='<justification>' (deny-by-default; bare 1 rejected) — allows only on the human's tg-ctl approval, which is why timeout_ms is 960000 (tg-ctl's live-Telegram cap). on_error=open."
}
