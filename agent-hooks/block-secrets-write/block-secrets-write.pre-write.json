{
  "id": "block-secrets-write",
  "point": "pre-write",
  "cmd": "/ABSOLUTE/PATH/TO/agent-hooks/block-secrets-write/block_secrets_write.py",
  "priority": 5,
  "timeout_ms": 3000,
  "on_error": "closed",
  "description": "Blocks writing a file whose content contains a likely live secret — API keys, tokens, bearer credentials, or a private-key PEM block — into a tracked file. Catches the leak BEFORE it lands on disk (and before any commit). on_error=closed: if the scan can't run, deny the write rather than risk persisting a secret."
}
